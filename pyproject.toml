[project]
name = "work"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tl-kenmogneyoute-ece", email = "tiphainelaure.kenmogneyoute@edu.ece.fr" }
]
requires-python = ">=3.13"
dependencies = [
    "faker>=40.39.0",
]

[project.scripts]
work = "work:main"
dataset-users = "work.dataset_users:main"
dataset-orders = "work.dataset_orders:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
